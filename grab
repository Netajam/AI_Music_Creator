#!/usr/bin/env bash
# Wrapper: downloads audio from YouTube / YouTube Music for use as a style reference.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec env PYTHONPATH="$ROOT${PYTHONPATH:+:$PYTHONPATH}" python3 -m aimc.references.grab "$@"
